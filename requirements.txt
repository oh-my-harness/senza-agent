# senza-agent runtime dependencies for Windows installer.
# The installer's setup_python.ps1 uses this to pip install into a venv.
#
# senza-sdk is published to PyPI as "senza-sdk".  If building from a local
# wheel instead, replace the line below with the wheel path or remove it
# and set SENZA_WHEEL env var before running setup_python.ps1.
senza-sdk>=1.2.3
aiohttp
httpx
